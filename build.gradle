#!groovy
// The above triggers groovy syntax highlighting in vim

plugins {
  id "org.jetbrains.intellij" version "0.0.39"
}

apply plugin: 'org.jetbrains.intellij'

group = 'com.samebug.clients.idea'
description = 'Samebug for IntelliJ IDEA'

sourceCompatibility = 1.6
targetCompatibility = 1.6

intellij {
  if (project.hasProperty("ijVersion")){
    version ijVersion
  } else {
    version '15.0.1'
  }
  pluginName 'Samebug'
  updateSinceUntilBuild false
}

repositories {
  jcenter()
  mavenLocal()
  mavenCentral()
}

dependencies {
  compile 'com.google.code.gson:gson:2.5'
  compile 'com.android.tools.ddms:ddmlib:24.5.0'
  testCompile 'junit:junit:4.11'
  testCompile 'org.mockito:mockito-core:1.10.19'
}
/*
samebug {
  properties {
    property 'samebug.projectName', 'Samebug for IntelliJ IDEA'
  }
}

license {
  strictCheck true
}

*/